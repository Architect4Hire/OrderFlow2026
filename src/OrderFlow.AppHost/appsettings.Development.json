{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Aspire.Hosting.Dcp": "Warning"
    }
  },
  "Parameters": {
    "//sql-password": "DEV-ONLY fallback so a fresh clone runs with no `dotnet user-secrets set` step. Never used outside Development; the real value comes from user-secrets or the environment.",
    "sql-password": "OrderFlow!Local1"
  }
}
